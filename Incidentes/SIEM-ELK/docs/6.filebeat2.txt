output:
  logstash:
    enabled: true
    hosts:
      - "172.20.0.10:5044"
    timeout: 15

filebeat:
  inputs:
    -
      paths:
        - /var/log/syslog
        - /var/log/auth.log
      fields:
        type: syslog
    -
      paths:
        - "/var/log/nginx/*.log"
      fields:
        type: nginx-access